import {SxProps} from "@mui/system";
import {Theme} from "@mui/material";

export const listLayout: SxProps<Theme> = {
    display: 'flex',
    gap: '16px',
    paddingTop: '16px',
    paddingBottom: '16px',
};
