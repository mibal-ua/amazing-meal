import { Theme } from "@mui/material";
import { SxProps } from "@mui/system";

export const centerWrapper: SxProps<Theme> = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    textAlign: "center",
    minHeight: "100vh"
};
