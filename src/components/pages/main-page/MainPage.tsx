import React from 'react';
import * as styles from './MainPage.styles';
import {Box, Button} from "@mui/material";

const MainPage = () => {
    return (
        <Box sx={styles.pageWrapper}>
            <Button color='primary'>ги</Button>
        </Box>
    );
};

export default MainPage;
